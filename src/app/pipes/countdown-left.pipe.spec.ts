import { CountdownLeftPipe } from './countdown-left.pipe';

describe('CountdownLeftPipe', () => {
  it('create an instance', () => {
    const pipe = new CountdownLeftPipe();
    expect(pipe).toBeTruthy();
  });
});
